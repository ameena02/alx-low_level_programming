#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
//*length of the password to be generated

	int N = 10;
	randomPasswordGeneration(N);

	return (0);
}
